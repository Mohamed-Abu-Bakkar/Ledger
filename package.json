{
  "name": "ledger-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd ledger-frontend && npm install && npm run build",
    "start": "node ledger-backend/server.js",
    "client": "cd ledger-frontend && npm run dev"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "4.18",
    "path-to-regexp": "^6.2.1",
    "react-router-dom": "^6.30.1",
    "vite": "^5.0.0"
  }
}
